#include <stdio.h>

int main(void)
{
 Declare an integer array(a)and initialize it with values
 int (void) = (10, 20, 30, 40, 50);
 Print the value of a[2] followed by a new lin
 printf("a[2]" = %d\n, a(2)); This line prints(a[2] = 30);
 return (0);
}
